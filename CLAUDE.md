## Agent skills

### Issue tracker

GitHub Issues on `alejo190404/legal-gate`, via `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Multi-context: root `CONTEXT-MAP.md` + per-service `CONTEXT.md` under `services/<name>/`. See `docs/agents/domain.md`.
